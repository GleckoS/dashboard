'use client';
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2198CE",
    },
    secondary: {
      main: "#E0E0E0",
    },
  },
});

export default theme;
